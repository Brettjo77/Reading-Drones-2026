# AGENTS.md

Canonical agent instructions for this repo live in [.github/copilot-instructions.md](.github/copilot-instructions.md). Read that file first — it covers project layout, the `window.*` module pattern, routing, styling conventions, and build/deploy.

If you update conventions, update **both** this pointer (if the canonical location moves) and `.github/copilot-instructions.md`.
